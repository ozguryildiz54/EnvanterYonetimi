@*@using EnvanterYonetimi.Models.Entity
@model List<Modeller>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<table class="table table-striped">
    <thead>
        <tr>
            <th scope="col">Model Id</th>
            <th scope="col">Model Adı</th>
            <th scope="col">Cihaz Turu Id</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var modeller in Model)
        {
            <tr>
                <th>@modeller.modelId</th>
                <td>@modeller.model</td>
                <td>@modeller.cihazTuruId</td>
            </tr>
        }
    </tbody>
</table>*@


